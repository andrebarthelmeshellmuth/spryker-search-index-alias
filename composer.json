{
    "name": "spryker-community/search-index-alias",
    "type": "library",
    "description": "Zed-driven blue/green search indices behind aliases: rebuild-then-atomic-flip, zero downtime including on first adoption, for multi-store/multi-locale Spryker catalogs.",
    "license": "MIT",
    "keywords": [
        "spryker",
        "elasticsearch",
        "opensearch",
        "search",
        "alias",
        "reindex",
        "blue-green",
        "zero-downtime"
    ],
    "require": {
        "php": ">=8.3",
        "guzzlehttp/guzzle": "^7.15.0",
        "php-amqplib/php-amqplib": "^3.7.0",
        "ruflin/elastica": "^7.3.0",
        "spryker/acl": "^3.27.0",
        "spryker/kernel": "^3.86.0",
        "spryker/propel-orm": "^1.25.0",
        "spryker/queue": "^1.29.1",
        "spryker/rabbit-mq": "^2.25.0",
        "spryker/search-elasticsearch": "^1.23.0",
        "spryker/store": "^1.37.0",
        "spryker/transfer": "^3.44.0",
        "spryker/translator": "^1.15.0"
    },
    "require-dev": {
        "codeception/codeception": "^5.0.0",
        "codeception/module-asserts": "^3.0.0",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0.0",
        "phpmd/phpmd": "^2.15.0",
        "phpstan/phpstan": "^2.0.0",
        "phpunit/phpunit": "^10.0.0 || ^11.0.0",
        "rector/rector": "^2.0",
        "slevomat/coding-standard": "8.22.1",
        "spryker/code-sniffer": "0.17.36",
        "squizlabs/php_codesniffer": "3.13.6"
    },
    "scripts": {
        "check-floors": [
            "@composer update --prefer-lowest --prefer-stable --no-dev --no-interaction",
            "php tools/floor-check.php"
        ],
        "rector": "rector process",
        "rector-dry-run": "rector process --dry-run",
        "phpstan": "phpstan analyse -c phpstan.neon src/",
        "phpstan-ci": [
            "php tests/_ci-standalone/generate-transfers.php",
            "phpstan analyse -c phpstan.ci.neon src/"
        ],
        "test-portable": [
            "php tests/_ci-standalone/generate-transfers.php",
            "codecept run -c tests/codeception.portable.yml -g Portable"
        ]
    },
    "scripts-descriptions": {
        "check-floors": "Verifies the declared dependency floors are real: resolves every constraint to its oldest allowed version (production deps only) and asserts each vendor symbol used in src/ actually exists there.",
        "rector": "Applies Rector's configured rule sets (PHP floor upgrades + level-0 dead-code/code-quality) to src/, tests/, and tools/.",
        "rector-dry-run": "Shows what Rector would change without writing anything.",
        "phpstan": "Static analysis at level 8. Must be run from a host shop (see README) — needs the shop's generated Generated\\Shared\\Transfer\\* classes and a freshly regenerated Ide/AutoCompletion stub, neither of which this package can produce standalone.",
        "phpstan-ci": "Standalone CI variant of the phpstan check: generates just the Transfer classes this package needs (same generator the Portable test suite uses) and ignores the two categories of class that only exist inside a real host shop (Propel Orm\\Zed\\* models, the aggregated Ide\\AutoCompletion stub) — see phpstan.ci.neon for exactly what's excluded and why.",
        "test-portable": "Runs this package's own @group Portable test subset standalone — no host shop, no live database, no search engine. See README, 'Testing and CI'."
    },
    "autoload": {
        "psr-4": {
            "SprykerCommunity\\": "src/SprykerCommunity/",
            "SprykerCommunityTest\\Zed\\SearchIndexAlias\\Business\\Deploy\\Fixture\\": "tests/SprykerCommunityTest/Zed/SearchIndexAlias/Business/Deploy/Fixture/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "SprykerCommunityTest\\": "tests/SprykerCommunityTest/",
            "Generated\\": [
                "src/Generated/",
                "tests/_ci-standalone/Generated/"
            ]
        }
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "minimum-stability": "stable",
    "homepage": "https://github.com/andrebarthelmeshellmuth/spryker-search-index-alias",
    "authors": [
        {
            "name": "Andre Barthelmes",
            "role": "Developer"
        }
    ],
    "support": {
        "issues": "https://github.com/andrebarthelmeshellmuth/spryker-search-index-alias/issues",
        "source": "https://github.com/andrebarthelmeshellmuth/spryker-search-index-alias"
    }
}
